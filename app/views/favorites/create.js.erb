$("#favorite-btn-<%= @book.id %>").html("<%= escape_javascript(render partial: 'favorites/favorite-btn', locals: {book: @book}) %>");
<!--インスタンス変数は、favorites_controllerのcreateアクションで定義されたものが使用できる。rubyのファイルなので、rubyの書き方にするために<>の中身の書き方になっている.-->